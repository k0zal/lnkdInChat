/*1. Use the "Run Javascript" extension on Google Chrome.
2. Paste the code into the extension while @ LinkedIn and press the Save & Run button.
3. PROFIT! or not, i cant remember ever using the chat.*/                               

      
let input = document.querySelector(".msg-form__contenteditable");

input.addEventListener("keyup", function(event) {
  if (event.keyCode === 13) {
   event.preventDefault();
   document.querySelector(".msg-form__send-button").click();
  }
});
